input {
  stdin {
    type => "human"
  }
}

output {
  stdout { debug => true debug_format => "json"}

  elasticsearch {
    #cluster => "logstash"
    bind_host => "{{ with_indexer_ip }}"
    host => "{{ with_elasticsearch_ip }}"
  }
}
